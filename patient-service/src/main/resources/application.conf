ktor {
  deployment {
    port = 8082
    port = ${?PORT}
  }
  application {
    modules = [com.example.ApplicationKt.module]
  }
}
jwt {
  domain = "https://jwt-provider-domain/"
  audience = "jwt-audience"
  realm = "ktor sample app"
  secret = "your-super-secret-key-must-be-at-least-256-bits-long-for-hs512-algorithm"
  expiration = 86400
  issuer = "med-collab-system"
}
database {
  mysql {
    url = "jdbc:h2:file:./.data/patient-service;MODE=MySQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1"
    url = ${?PATIENT_DATABASE_URL}
    user = "sa"
    user = ${?PATIENT_DATABASE_USER}
    password = ""
    password = ${?PATIENT_DATABASE_PASSWORD}
    driver = "org.h2.Driver"
    driver = ${?PATIENT_DATABASE_DRIVER}
  }
}
