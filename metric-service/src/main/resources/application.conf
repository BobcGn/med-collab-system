ktor {
  deployment {
    port = 8083
    port = ${?PORT}
  }
  application {
    modules = [com.example.ApplicationKt.module]
  }
}
jwt {
  domain = "https://jwt-provider-domain/"
  audience = "jwt-audience"
  realm = "ktor sample app"
  secret = "your-super-secret-key-must-be-at-least-256-bits-long-for-hs512-algorithm"
  expiration = 86400
  issuer = "med-collab-system"
}
database {
  mysql {
    url = "jdbc:mysql://localhost:4000/analysis?useInformationSchema=false"
    url = ${?METRIC_DATABASE_URL}
    user = "Hyy"
    user = ${?METRIC_DATABASE_USER}
    password = "hyy20060420"
    password = ${?METRIC_DATABASE_PASSWORD}
    driver = "com.mysql.cj.jdbc.Driver"
    driver = ${?METRIC_DATABASE_DRIVER}
  }
}
deepseek {
  apiKey = ${?DEEPSEEK_API_KEY}
}
segmentation {
  enabled = false
  enabled = ${?METRIC_SEGMENTATION_SERVICE_ENABLED}
  baseUrl = "http://127.0.0.1:8099"
  baseUrl = ${?METRIC_SEGMENTATION_SERVICE_URL}
  timeoutSeconds = 30
  timeoutSeconds = ${?METRIC_SEGMENTATION_SERVICE_TIMEOUT_SECONDS}
}
