ktor {
  deployment {
    port = 8083
    port = ${?PORT}
  }
  application {
    modules = [com.example.medcollab.metric.ApplicationKt.module]
  }
}
database {
  mysql {
    url = "jdbc:mysql://mysql:3306/ktor_database?useSSL=false"
    user = "root"
    password = "password"
    driver = "com.mysql.cj.jdbc.Driver"
  }
}
