ktor {
  deployment {
    port = 8081
    port = ${?PORT}
  }
  application {
    modules = [com.example.ApplicationKt.module]
  }
}
jwt {
  audience = "jwt-audience"
  realm = "ktor sample app"
  secret = "your-super-secret-key-must-be-at-least-256-bits-long-for-hs512-algorithm"
  expiration = 86400
  issuer = "med-collab-system"
}
database {
  mysql {
    url = "jdbc:mysql://localhost:4000/med_auth?useInformationSchema=false"
    url = ${?AUTH_DATABASE_URL}
    user = "Hyy"
    user = ${?AUTH_DATABASE_USER}
    password = "hyy20060420"
    password = ${?AUTH_DATABASE_PASSWORD}
    driver = "com.mysql.cj.jdbc.Driver"
    driver = ${?AUTH_DATABASE_DRIVER}
  }
}
